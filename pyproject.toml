[project]
name = "pos-encode"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "einops>=0.8.1",
    "torch>=2.9.1",
]

[dependency-groups]
dev = [
    "ruff>=0.14.6",
]
